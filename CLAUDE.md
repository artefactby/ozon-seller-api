# CLAUDE.md

Compatibility shim for Claude Code.

The repository's agent instructions now live in `AGENTS.md`.
Maintainer and publishing workflow now live in `MAINTAINER.md`.

Treat `AGENTS.md` as the source of truth.
